import { AppUserDirective } from './app-user.directive';

describe('AppUserDirective', () => {
  it('should create an instance', () => {
    const directive = new AppUserDirective();
    expect(directive).toBeTruthy();
  });
});
